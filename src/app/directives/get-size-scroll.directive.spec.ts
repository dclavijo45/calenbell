import { GetSizeScrollDirective } from './get-size-scroll.directive';

describe('GetSizeScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new GetSizeScrollDirective();
    expect(directive).toBeTruthy();
  });
});
