import { SetLocationScrollDirective } from './set-location-scroll.directive';

describe('SetLocationScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new SetLocationScrollDirective();
    expect(directive).toBeTruthy();
  });
});
